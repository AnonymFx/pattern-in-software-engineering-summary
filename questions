Thomas:
1. MVC: Views invoke changes in model? Not controller? If not controller, what is it for then?

Nils:

Bene:
@Thomas: 1. My interpretation: invoke != apply. View invokes change, calls controller which executes transformation.
